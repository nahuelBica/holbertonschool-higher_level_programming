#!/usr/bin/python3
"""
Function used to check if an object is an instance of a class
or inheriths of them
"""


def is_kind_of_class(obj, a_class):
    """
    Function used to check if an object is an instance of a class
    or inheriths of them
    """
    return isinstance(obj, a_class)
